/**
 * 描述
 * @date 2020-12-08
 * @param {any} request
 * @param {any} response
 * @returns {any}
 */
module.exports.index = (request,response)=>{
    response.json({
        "message":"Hello"
    });
};